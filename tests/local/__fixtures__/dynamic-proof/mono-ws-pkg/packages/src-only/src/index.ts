// SOURCE-only sibling entry (package.json `main` points here; no built output). Aspect-2 copies this
// `src` + tsconfig into the sandbox and aliases the package name to this file so the runner transforms it.
export const greet = 7;
